default:
  tags:
    - tour-runner

image: mcr.microsoft.com/dotnet/core/sdk:3.1

stages:
  - build
  - test
  - release

before_script:
  - "cd EPAM.DreamTour"
  - "dotnet restore"

build:
  stage: build
  script:
    - dotnet build

test:
  stage: test
  script:
    - dotnet test
